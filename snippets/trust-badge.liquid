{%- comment -%}
  Trust Badge snippet
  Usage: {% render 'trust-badge', text: 'Science-backed' %}
{%- endcomment -%}

<div class="trust-badge">
  <span class="trust-badge__icon" aria-hidden="true">✓</span>
  <span>{{ text }}</span>
</div>
