<section style="min-height: calc(100vh - var(--header-height) - 200px); display: flex; align-items: center; justify-content: center; text-align: center; padding: 64px 24px;">
  <div style="max-width: 480px;">
    <div style="width: 72px; height: 72px; background: var(--color-mint-mist); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 24px; color: var(--color-primary);">
      <i data-lucide="search-x"></i>
    </div>
    <h1 style="font-family: var(--font-heading); font-size: clamp(1.75rem, 4vw, 2.25rem); color: var(--color-text); margin-bottom: 12px;">Page Not Found</h1>
    <p style="font-size: 15px; color: var(--color-text-body); line-height: 1.6; margin-bottom: 32px;">The page you're looking for doesn't exist or has been moved. Let's get you back on track.</p>
    <div style="display: flex; gap: 12px; justify-content: center; flex-wrap: wrap;">
      <a href="/pages/quiz" class="btn btn--primary">Take the Quiz</a>
      <a href="/" class="btn btn--outline">Go Home</a>
    </div>
  </div>
</section>

<style>
  .template-404 [data-lucide="search-x"] {
    width: 32px;
    height: 32px;
    stroke-width: 1.5;
  }
</style>
