{{ 'product.css' | asset_url | stylesheet_tag }}

{% render 'json-ld-product', product: product %}
{% render 'json-ld-faq' %}
{% render 'json-ld-breadcrumbs' %}

<div class="product-page">
  <!-- Product Hero -->
  <section class="product-hero">
    <div class="container">
      <div class="product-hero__grid">
        <!-- Product Image -->
        <div class="product-hero__image">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.featured_image.alt | default: product.title }}" loading="lazy" width="600">
          {% else %}
            <div class="product-hero__placeholder">
              <i data-lucide="pill"></i>
            </div>
          {% endif %}
        </div>

        <!-- Product Info -->
        <div class="product-hero__info">
          {% if product.metafields.custom.category %}
            <span class="label mb-sm">{{ product.metafields.custom.category }}</span>
          {% endif %}

          <h1 class="product-hero__title">{{ product.title }}</h1>

          <div class="product-hero__rating">
            <span class="product-hero__stars">&#9733;&#9733;&#9733;&#9733;&#9733;</span>
            <span class="product-hero__reviews-count">4.8 (127 reviews)</span>
          </div>

          <p class="product-hero__price">{{ product.price | money }}</p>

          {% if product.description != blank %}
            <div class="product-hero__description">
              {{ product.description }}
            </div>
          {% endif %}

          <!-- Add to Cart -->
          {% form 'product', product %}
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
            <button
              type="submit"
              class="btn btn--primary btn--lg product-hero__atc"
              data-add-to-cart
              data-product-handle="{{ product.handle }}"
              data-product-title="{{ product.title }}"
              data-product-price="{{ product.price }}"
              {% unless product.available %}disabled{% endunless %}
            >
              {% if product.available %}
                <i data-lucide="shopping-bag"></i>
                Add to Cart
              {% else %}
                Sold Out
              {% endif %}
            </button>
          {% endform %}

          <!-- Guarantee Badge -->
          <div class="product-guarantee">
            <i data-lucide="shield-check"></i>
            <div>
              <strong>30-Day Money-Back Guarantee</strong>
              <span>No questions asked</span>
            </div>
          </div>

          <!-- Trust Row -->
          <div class="product-trust">
            <div class="product-trust__item">
              <i data-lucide="shield-check"></i>
              <span>Third-party tested</span>
            </div>
            <div class="product-trust__item">
              <i data-lucide="leaf"></i>
              <span>No fillers</span>
            </div>
            <div class="product-trust__item">
              <i data-lucide="truck"></i>
              {% render 'locale-vars' %}<span>{{ free_shipping_short }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Ingredients Section -->
  {% if product.metafields.custom.ingredients %}
    <section class="product-ingredients section-padding">
      <div class="container container--narrow">
        <h2 class="product-section__heading">What's Inside</h2>
        <div class="product-ingredients__list">
          {% for ingredient in product.metafields.custom.ingredients.value %}
            <div class="product-ingredient">
              <div class="product-ingredient__icon">
                <i data-lucide="check"></i>
              </div>
              <div>
                <span class="product-ingredient__name">{{ ingredient.name }}</span>
                {% if ingredient.dose %}
                  <span class="product-ingredient__dose">{{ ingredient.dose }}</span>
                {% endif %}
                {% if ingredient.benefit %}
                  <p class="product-ingredient__benefit">{{ ingredient.benefit }}</p>
                {% endif %}
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </section>
  {% endif %}

  <!-- How to Take -->
  <section class="product-usage section-padding bg-mint">
    <div class="container container--narrow">
      <h2 class="product-section__heading">How to Take</h2>
      <div class="product-usage__card">
        <div class="product-usage__item">
          <i data-lucide="calendar"></i>
          <div>
            <strong>Daily Use</strong>
            <p>Take daily for best results. Consistency is key.</p>
          </div>
        </div>
        <div class="product-usage__item">
          <i data-lucide="clock"></i>
          <div>
            <strong>When to Take</strong>
            <p>{{ product.metafields.custom.timing | default: "Take with food, preferably in the morning." }}</p>
          </div>
        </div>
        <div class="product-usage__item">
          <i data-lucide="droplets"></i>
          <div>
            <strong>Dosage</strong>
            <p>{{ product.metafields.custom.dosage | default: "Follow the serving size on the label." }}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Product Facts -->
  <section class="product-facts section-padding">
    <div class="container container--narrow">
      <h2 class="product-section__heading"><i data-lucide="clipboard-list" style="width:20px;height:20px;color:var(--color-primary);"></i> Product Facts</h2>
      <div class="product-facts__header">Size: 1 fl oz / 30 ml &middot; Cruelty-free &middot; Full INCI ingredient list below</div>
      <table class="product-facts__table">
        <thead>
          <tr><th>Key Active</th><th>Concentration / Role</th></tr>
        </thead>
        <tbody>
          <!-- Populated via product metafields in production -->
          <tr><td>See full ingredient list</td><td>\u2014</td></tr>
        </tbody>
      </table>
      <p style="font-size:12px; color:var(--color-text-muted); margin-top:8px;">Patch-test before first use. For external use only.</p>
    </div>
  </section>

  <!-- Backed by Science -->
  <section class="product-studies section-padding bg-mint">
    <div class="container container--narrow">
      <h2 class="product-section__heading"><i data-lucide="flask-conical" style="width:20px;height:20px;color:var(--color-primary);"></i> Backed by Science</h2>
      <div class="product-studies__list">
        <!-- Studies populated via product metafields in production -->
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="product-faq section-padding">
    <div class="container container--narrow">
      <h2 class="product-section__heading"><i data-lucide="help-circle" style="width:20px;height:20px;color:var(--color-primary);"></i> Frequently Asked Questions</h2>
      <div class="product-faq__list">
        <div class="product-faq__item">
          <button class="product-faq__question" type="button">When will I see results? <i data-lucide="chevron-down"></i></button>
          <div class="product-faq__answer"><div class="product-faq__answer-inner">Hydration and calming products usually feel different on day one. Brightening and anti-aging actives take 2-4 weeks to show visible changes, with full benefits building over 6-8 weeks of consistent use.</div></div>
        </div>
        <div class="product-faq__item">
          <button class="product-faq__question" type="button">Can I use this with other skincare products? <i data-lucide="chevron-down"></i></button>
          <div class="product-faq__answer"><div class="product-faq__answer-inner">Yes \u2014 every Nutrava product is designed to layer as part of a routine. A good rule: apply thinnest to thickest, water-based serums before oils, SPF last in the morning. If you use prescription skincare, please consult your dermatologist first.</div></div>
        </div>
        <div class="product-faq__item">
          <button class="product-faq__question" type="button">Are there any side effects? <i data-lucide="chevron-down"></i></button>
          <div class="product-faq__answer"><div class="product-faq__answer-inner">Our formulas use clinically tested ingredients at safe concentrations. Mild tingling or flushing can happen when introducing new actives like retinol or vitamin C. Always patch-test before first use and discontinue if you experience irritation.</div></div>
        </div>
        <div class="product-faq__item">
          <button class="product-faq__question" type="button">How should I store this? <i data-lucide="chevron-down"></i></button>
          <div class="product-faq__answer"><div class="product-faq__answer-inner">Store in a cool, dry place away from direct sunlight. Keep the lid tightly closed to preserve active ingredients.</div></div>
        </div>
        <div class="product-faq__item">
          <button class="product-faq__question" type="button">Is this cruelty-free and vegan? <i data-lucide="chevron-down"></i></button>
          <div class="product-faq__answer"><div class="product-faq__answer-inner">All Nutrava products are cruelty-free and never tested on animals. The vast majority are also vegan \u2014 the full INCI list above confirms what\u2019s in each formula.</div></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Launch Offer Banner (replaces reviews until real ones exist) -->
  <section class="section-padding bg-mint">
    <div class="container container--narrow" style="text-align:center;">
      <p style="font-size:1rem; font-weight:600; color:var(--color-text); margin-bottom:8px;">Launch Special</p>
      <p style="font-size:0.95rem; color:var(--color-text-body);">Use code <strong>LAUNCH20</strong> for 20% off your first order.</p>
    </div>
  </section>

  <script>
  (function() {
    var dataEl = document.getElementById('product-reviews-data');
    if (!dataEl) return;
    var data = JSON.parse(dataEl.textContent);
    var container = document.querySelector('[data-reviews-container]');
    if (!container) return;

    var stars = function(n) { var s = ''; for (var i = 0; i < 5; i++) s += i < n ? '\u2605' : '\u2606'; return s; };

    var html = '<div class="product-reviews__summary">' +
      '<div class="product-reviews__score">' +
        '<span class="product-reviews__number">' + data.rating + '</span>' +
        '<span class="product-reviews__stars-big">' + stars(Math.round(data.rating)) + '</span>' +
        '<span class="product-reviews__total">Based on ' + data.count + ' reviews</span>' +
      '</div>' +
      '<div class="product-reviews__bars">';

    for (var i = 0; i < 5; i++) {
      var label = (5 - i) + ' \u2605';
      var pct = data.distribution[i] + '%';
      html += '<div class="product-reviews__bar-row"><span class="product-reviews__bar-label">' + label + '</span><div class="product-reviews__bar"><div class="product-reviews__bar-fill" style="width:' + pct + '"></div></div><span class="product-reviews__bar-pct">' + pct + '</span></div>';
    }
    html += '</div></div>';

    data.reviews.forEach(function(r) {
      html += '<div class="product-review">' +
        '<div class="product-review__header"><span class="product-review__author">' + r.author + '</span><span class="product-review__date">' + r.date + '</span></div>' +
        '<span class="product-review__stars">' + stars(r.rating) + '</span>' +
        (r.verified ? '<span class="product-review__verified">Verified Purchase</span>' : '') +
        '<p class="product-review__text">' + r.text + '</p>' +
      '</div>';
    });

    container.innerHTML = html;
  })();
  </script>

  <!-- Sticky Add to Cart Bar -->
  <div class="sticky-atc" data-sticky-atc>
    <div class="container">
      <div class="sticky-atc__inner">
        <div class="sticky-atc__info">
          <span class="sticky-atc__name">{{ product.title }}</span>
          <span class="sticky-atc__price" data-sticky-price>{{ product.price | money }}</span>
        </div>
        <button
          class="btn btn--primary btn--sm sticky-atc__btn"
          data-sticky-add-to-cart
          data-product-handle="{{ product.handle }}"
          data-product-title="{{ product.title }}"
          data-product-price="{{ product.price }}"
        >
          Add to Cart
        </button>
      </div>
    </div>
  </div>

  <!-- Related Products -->
  {% if product.collections.first %}
    <section class="product-related section-padding">
      <div class="container">
        <h2 class="product-section__heading text-center">Complete Your Routine</h2>
        <div class="product-related__grid">
          {% for related in product.collections.first.products limit: 4 %}
            {% unless related.handle == product.handle %}
              {% render 'product-card', product: related %}
            {% endunless %}
          {% endfor %}
        </div>
      </div>
    </section>
  {% endif %}
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    /* ---------- Analytics: view_item ---------- */
    if (typeof gtag === 'function') gtag('event', 'view_item', { currency: 'USD', items: [{ item_name: '{{ product.title | escape }}', item_id: '{{ product.handle }}', price: {{ product.price | divided_by: 100.0 }} }] });
    if (typeof fbq === 'function') fbq('track', 'ViewContent', { content_name: '{{ product.title | escape }}', content_ids: ['{{ product.handle }}'], content_type: 'product', value: {{ product.price | divided_by: 100.0 }}, currency: 'USD' });

    /* ---------- AJAX Add to Cart ---------- */
    var mainBtn = document.querySelector('[data-add-to-cart]');
    if (mainBtn) {
      mainBtn.closest('form').addEventListener('submit', function(e) {
        e.preventDefault();
        var btn = mainBtn;
        var handle = btn.dataset.productHandle;
        var title = btn.dataset.productTitle;
        var price = btn.dataset.productPrice;
        var variantId = this.querySelector('[name="id"]').value;

        NutravaCart.addItem({
          id: parseInt(variantId),
          handle: handle,
          title: title,
          price: parseInt(price),
          quantity: 1
        });
      });
    }

    /* ---------- Sticky Add to Cart ---------- */
    var stickyBar = document.querySelector('[data-sticky-atc]');
    var stickyBtn = document.querySelector('[data-sticky-add-to-cart]');
    if (mainBtn && stickyBar) {
      var observer = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          stickyBar.classList.toggle('is-visible', !entry.isIntersecting);
        });
      }, { threshold: 0 });
      observer.observe(mainBtn);

      if (stickyBtn) {
        stickyBtn.addEventListener('click', function() {
          var handle = this.dataset.productHandle;
          var title = this.dataset.productTitle;
          var price = this.dataset.productPrice;
          NutravaCart.addItem({
            id: Date.now(),
            handle: handle,
            title: title,
            price: parseInt(price),
            quantity: 1
          });
        });
      }
    }

    /* ---------- FAQ Accordion ---------- */
    document.querySelectorAll('.product-faq__question').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var answer = btn.nextElementSibling;
        var isOpen = btn.classList.contains('is-open');
        btn.closest('.product-faq__list').querySelectorAll('.product-faq__question').forEach(function(q) {
          q.classList.remove('is-open');
          q.nextElementSibling.style.maxHeight = null;
        });
        if (!isOpen) {
          btn.classList.add('is-open');
          answer.style.maxHeight = answer.scrollHeight + 'px';
        }
      });
    });

  });
</script>
