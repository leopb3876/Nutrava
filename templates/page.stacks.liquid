<section style="padding: 80px 0; background: linear-gradient(180deg, var(--color-mint-mist) 0%, var(--color-bg) 100%); text-align: center;">
  <div class="container container--narrow">
    <span class="label mb-sm" style="display: block;">Explore</span>
    <h1 style="font-family: var(--font-heading); font-size: clamp(2rem, 4vw, 2.5rem); color: var(--color-text); margin-bottom: 16px;">Browse Our Routines</h1>
    <p style="font-size: 1rem; color: var(--color-text-body); line-height: 1.6; max-width: 480px; margin: 0 auto;">Find a routine built for your skin concern. Each routine is a curated set of products designed to layer together.</p>
  </div>
</section>

<section style="padding: 64px 0;">
  <div class="container">
    <div class="stacks-grid">
      <div class="stack-card">
        <div class="stack-card__icon">
          <i data-lucide="droplet"></i>
        </div>
        <h3 class="stack-card__title">Acne & Blemishes</h3>
        <p class="stack-card__desc">Clear breakouts without stripping your skin.</p>
        <span class="stack-card__count">2–4 products</span>
        <a href="/collections/acne" class="btn btn--outline btn--sm stack-card__btn">View Routine</a>
      </div>

      <div class="stack-card">
        <div class="stack-card__icon">
          <i data-lucide="sparkles"></i>
        </div>
        <h3 class="stack-card__title">Anti-Aging</h3>
        <p class="stack-card__desc">Smooth fine lines, support firmness, and maintain elasticity.</p>
        <span class="stack-card__count">2–4 products</span>
        <a href="/collections/anti-aging" class="btn btn--outline btn--sm stack-card__btn">View Routine</a>
      </div>

      <div class="stack-card">
        <div class="stack-card__icon">
          <i data-lucide="glass-water"></i>
        </div>
        <h3 class="stack-card__title">Hydration</h3>
        <p class="stack-card__desc">Relieve tightness and lock in all-day moisture.</p>
        <span class="stack-card__count">2–4 products</span>
        <a href="/collections/hydration" class="btn btn--outline btn--sm stack-card__btn">View Routine</a>
      </div>

      <div class="stack-card">
        <div class="stack-card__icon">
          <i data-lucide="sun"></i>
        </div>
        <h3 class="stack-card__title">Brightening</h3>
        <p class="stack-card__desc">Fade dark spots and bring back your natural glow.</p>
        <span class="stack-card__count">2–4 products</span>
        <a href="/collections/brightening" class="btn btn--outline btn--sm stack-card__btn">View Routine</a>
      </div>

      <div class="stack-card">
        <div class="stack-card__icon">
          <i data-lucide="feather"></i>
        </div>
        <h3 class="stack-card__title">Sensitive Skin</h3>
        <p class="stack-card__desc">Calm reactive skin with gentle, fragrance-free formulas.</p>
        <span class="stack-card__count">2–4 products</span>
        <a href="/collections/sensitive" class="btn btn--outline btn--sm stack-card__btn">View Routine</a>
      </div>
    </div>

    <!-- Bottom CTA -->
    <div class="stacks-cta">
      <h3 style="font-family: var(--font-heading); font-size: 1.25rem; color: var(--color-text); margin-bottom: 8px;">Not sure which routine?</h3>
      <p style="font-size: 15px; color: var(--color-text-body); margin-bottom: 20px;">Take our quiz and we'll match you with the right routine in under 60 seconds.</p>
      <a href="/pages/quiz" class="btn btn--primary">Take the Quiz</a>
    </div>
  </div>
</section>

<style>
  .stacks-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    max-width: 1000px;
    margin: 0 auto;
  }

  .stack-card {
    background: var(--color-card-bg);
    border: 1.5px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    text-align: center;
    transition: all var(--transition-base);
    cursor: pointer;
  }

  .stack-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .stack-card__icon {
    width: 64px;
    height: 64px;
    background: var(--color-mint-mist);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-lg);
    color: var(--color-primary);
  }

  .stack-card__icon svg {
    width: 28px;
    height: 28px;
  }

  .stack-card__title {
    font-family: var(--font-heading);
    font-size: 1.2rem;
    color: var(--color-text);
    margin-bottom: var(--space-sm);
  }

  .stack-card__desc {
    font-size: 14px;
    color: var(--color-text-muted);
    line-height: 1.5;
    margin-bottom: var(--space-md);
  }

  .stack-card__count {
    font-size: 12px;
    color: var(--color-primary);
    font-weight: 600;
    display: block;
    margin-bottom: var(--space-lg);
  }

  .stack-card__btn {
    width: 100%;
  }

  .stacks-cta {
    text-align: center;
    margin-top: var(--space-2xl);
    padding: var(--space-xl);
    background: var(--color-mint-mist);
    border-radius: var(--radius-lg);
    max-width: 1000px;
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 768px) {
    .stacks-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .stacks-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
