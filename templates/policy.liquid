<section class="sn-page-hero">
  <span class="sn-page-hero__eyebrow">Legal</span>
  <h1 class="sn-page-hero__title">{{ policy.title }}</h1>
</section>

<section class="sn-page-section sn-page-section--narrow">
  <div class="sn-prose rte">
    {{ policy.body }}
  </div>
</section>
